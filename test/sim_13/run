source ../test_functions.bsh

setup

set_simulators

for s in ${SVUNIT_SIMULATORS[@]}; do
  runSVUnit -s $s

  expect_string "ERROR: \[50\]\[dut_ut\]: fail_if: svunit_timeout" run.log &&
  expect_string "INFO:  \[99\]\[dut_ut\]: no_timeout::PASSED" run.log
done

exit 0

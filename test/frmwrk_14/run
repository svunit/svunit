source ../test_functions.bsh
set_simulators

NEWPATH=`echo $PATH | sed -e "s/svunit-code\/bin//"`
export PATH=$NEWPATH
export SVUNIT_INSTALL=

csh run.csh ${SVUNIT_SIMULATORS[0]}
if [ "$?" != "0" ]; then
  exit 1
fi

tcsh run.csh ${SVUNIT_SIMULATORS[0]}
if [ "$?" != "0" ]; then
  exit 1
fi

exit 0

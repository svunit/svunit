source ../test_functions.bsh

# create the Makefile
runSVUnit -s modelsim -r "+JOKES +DUD=4" -r_arg "+BOZO"

# check the log output for a PASS from the testrunner
expect_testrunner_pass run.log

exit 0
